{
  "private": true,
  "scripts": {
    "sync:assets": "python3 scripts/sync_static_versions.py",
    "check:assets": "python3 scripts/sync_static_versions.py --check",
    "test:browser": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1"
  }
}
